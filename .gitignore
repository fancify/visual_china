node_modules/
dist/

# Claude Code subagent worktrees — temporary, never committed.
.claude/worktrees/

# Raw DEM downloads and extracted source tiles are too large for Git.
data/fabdem/
data/etopo/
data/gshhg/

# Raw third-party hydrography downloads are reproducible source data.
data/hydrography/raw/

# Natural Earth 10m vectors — downloaded on demand by build-china-major-* scripts.
data/natural-earth/

.DS_Store
*.log
.env
.env.*

# Playwright MCP screenshot debris (one-off iteration captures, not assets).
/*.jpeg
/*.png
.playwright-mcp/
atlas-snapshot.md

tmp/
.claude/

# SFX raw downloads + transcoded ogg files; manifest is the source of truth.
# 跑 `node scripts/download-sfx.mjs` 重新生成。
public/sfx/*/*.opus
public/sfx/*/*.ogg
data/hydrosheds/
public/data/qinling-slice-dem.*.bin

# 大体量 DEM JSON：reproducible via `npm run build:dem`，不进 git（GitHub 100MB 限制）
public/data/qinling-slice-dem.json
public/data/regions/qinling/slice-l1.json
.context/

# Test runner temp dirs (sky-atmosphere, avatar, mount, scenic-poi 测试用)
.codex-temp-tests/
logs/
.obsidian/

# Legacy main.ts demo 用的 pre-baked chunks (2.1 GB)。pyramid-demo (active) 不加载
# 这些。如要 npm run dev index.html，得 `npm run build:chunks` 重新烤
public/data/regions/qinling/chunks/

# (2026-05-16 起 dem/ 和 rivers/ 进 git，让协作者 clone 后直接跑 demo 不用额外传数据)

# Tang 历史地理参考材料 (谭其骧地图集 + CHGIS) - 数 GB, 第三方版权
data/references/
